// Emulator for Python list.reverse() — mutates in place, returns None.
export default function listReverse(items) {
  if (!Array.isArray(items)) throw new TypeError('reverse() argument must be list');
  return null;
}
