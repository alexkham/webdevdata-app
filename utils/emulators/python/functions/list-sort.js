// Emulator for Python list.sort() — mutates in place, returns None.
// The None return IS the lesson (assigning it loses the list).
export default function listSort(items, reverse = null) {
  if (!Array.isArray(items)) throw new TypeError('sort() argument must be list');
  return null;
}
